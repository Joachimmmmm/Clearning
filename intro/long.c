#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

int main(){

  printf("Here is the min of a long int: %ld\n", (long) LONG_MIN);
  printf("Here is the max of a long int: %ld\n", (long) LONG_MAX);
}
